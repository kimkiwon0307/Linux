# 우분투 리눅스

## 1장. 실습 환경 구축

> **개요:** 리눅스 실습의 기반이 되는 가상화 개념을 이해하고, VMware Workstation을 활용한 가상머신 환경 구축 및 특징을 학습합니다.

---

### 1.1 가상머신 소개와 설치

#### 1.1.1 가상머신 소개
* **가상화 (Virtualization):** 실제 컴퓨터 한 대를 여러 대처럼 효율적으로 나누어 쓰기 위해 탄생한 기술입니다.
* **가상머신 (VM, Virtual Machine):** 실제 컴퓨터 1대 안에서 동시에 독립적으로 실행되는 가상 컴퓨터들을 의미합니다. (ex. Ubuntu, Rocky Linux, Windows Server 등)

#### 1.1.2 가상머신과 가상머신 소프트웨어
* **하이퍼바이저 (Hypervisor):** 가상머신(VM)을 생성하고 관리하는 프로그램입니다.
* **대표 제품군:** `VMware`, `Hyper-V`, `KVM`, `VirtualBox`
* *참고:* 실무에서 가장 사용률과 선호도가 높은 **VMware**를 기준으로 실습을 진행합니다.

#### 1.1.3 VMware Workstation Pro 설치
* VM 생성 및 삭제, 복사, 네트워크 관리, 스냅샷 관리 등 가상 인프라 전반을 제어하는 핵심 도구입니다.

---

### 1.2 가상머신 생성

#### 1.2.1 가상머신의 구조
가상머신은 실제 물리 컴퓨터의 자원을 빌려와 가짜 하드웨어를 구성하는 원리로 동작하며, 구조적으로 완벽히 매칭됩니다.

| 실제 컴퓨터 자원 | 가상 머신 (VM) 자원 |
| :--- | :--- |
| CPU | **vCPU** (Virtual CPU) |
| RAM | **vRAM** (Virtual RAM) |
| SSD / HDD | **vDISK** (Virtual Disk) |
| NIC (랜카드) | **vNIC** (Virtual Network Interface Card) |

#### 1.2.2 가상머신 만들기
* **프로비저닝 (Provisioning):** VM을 생성할 때 하드웨어 자원(CPU, RAM, Disk, Network)과 설치할 운영체제(OS ISO 파일)를 할당하고 준비하는 일련의 과정을 말합니다.

---

### 1.3 VMware의 핵심 특징

1. **격리성 (Isolation):** VM(Ubuntu) 내부가 바이러스나 오류로 망가지더라도 호스트 PC(Windows)에는 전혀 영향을 주지 않습니다.
2. **복제 (Clone):** 이미 구성이 완료된 서버 환경을 그대로 복사하여 똑같은 서버를 빠르게 찍어낼 수 있습니다.
3. **스냅샷 (Snapshot):** 현재 정상적인 서버 상태를 저장(백업)해 두고, 문제 발생 시 언제든 해당 시점으로 되돌릴 수 있습니다.
4. **다중 실행:** 컴퓨터 한 대의 자원을 나누어 여러 서버(Web, DB, DNS 서버 등)를 동시에 띄우고 테스트할 수 있습니다.

---

### 1.4 원활한 실습 준비

#### 1.4.1 VMware 단축키 (Hot Key)
* `Ctrl + G` : 호스트(Windows)에서 VM 화면 안으로 마우스/키보드 제어권 넘기기
* `Ctrl + Alt` : VM 안에서 빠져나와 호스트(Windows)로 제어권 돌리기

#### 1.4.2 종료 옵션 (전원 관리)
* **Suspend (일시정지):** 현재 작업 상태를 그대로 메모리에 저장하고 멈춥니다. 빠른 재개에 유용합니다.
* **Power Off (강제 종료):** 실제 컴퓨터의 전원 플러그를 뽑는 것과 같으므로 데이터 유실에 주의해야 합니다.

#### 1.4.3 전체 화면 사용 및 여러 VM 동시 실행 기능 활용
* 실무 환경과 유사한 다중 서버 아키텍처 구성을 위해 여러 VM을 동시에 제어하는 방법을 숙지합니다.

#### 1.4.4 가상 네트워크 종류 파악

| 종류 | 인터넷 연결 여부 | 특징 및 통신 범위 |
| :--- | :---: | :--- |
| **Bridged** | ⭕ 가능 | 실제 PC와 동일한 네트워크 대역의 IP를 공유기에서 직접 할당받음. PC와 VM이 대등한 독립 장비로 동작. |
| **NAT** | ⭕ 가능 | **[실습 주력]** 호스트 PC 내부 IP를 공유받아 사용. VM에서 인터넷은 가능하나, 외부에서 VM으로의 직접 접근은 차단되어 보안상 유리. |
| **Host-Only** | ❌ 불가 | 외부 인터넷과 단절된 폐쇄망 구축 시 사용. 호스트 PC와 가상머신들 사이에서만 내부 통신 가능. |

#### 1.4.5 파일 전송 방법 (Windows ➔ Linux)
1. Drag & Drop (드래그 앤 드롭)
2. Shared Folder (VMware 공유 폴더 설정)
3. **SCP** (Secure Copy Protocol) 🌟 *실무 다빈도*
4. **WinSCP** (GUI 기반의 FTP/SFTP 클라이언트 프로그램) 🌟 *실무 다빈도*
      
# 2장 우분투 리눅스 소개

> **개요:** 리눅스의 탄생 배경과 핵심 구성 요소인 커널(Kernel) 및 셸(Shell)의 개념을 이해하고, 우분투 배포판의 특징과 주요 패키지를 학습합니다.

---
  ### 2.1 리눅스의 개요

#### 2.1.1 리눅스의 탄생
* **초기 컴퓨터:** 운영체제(OS)가 없어 하드웨어에 프로그램을 직접 입력해야 하는 극심한 불편함이 존재했습니다.
* **UNIX의 등장:** 안정성, 멀티태스킹, 다중 사용자 지원을 무기로 혁명을 일으켰으나, 지나치게 높은 비용이 단점이었습니다.
* **Linux의 탄생:** 리누스 토발즈(Linus Torvalds)라는 대학생이 유닉스를 기반으로 취미 삼아 무료 운영체제 핵심인 **Linux Kernel**을 개발했습니다.
* **성공 요인:** 무료 이용 가능, 소스 코드 완전 공개(Open Source), 전 세계 개발자의 자유로운 수정 및 기여가 가능했기 때문입니다.

#### 2.1.2 GNU 프로젝트
* **개념:** "모든 사람이 자유롭게 사용할 수 있는 소프트웨어 생태계를 만들자"는 이념으로 시작된 프로젝트입니다.
* **성공과 공백:** `bash`, `gcc`, `gdb`, `coreutils` 등 운영체제에 필요한 핵심 도구들을 완벽히 만들었으나, 정작 시스템을 조율할 '커널'이 완성되지 못한 상태였습니다.
* **완성:** GNU의 다양한 오픈소스 도구들과 리누스 토발즈가 만든 **Linux Kernel**이 결합하면서 오늘날 우리가 사용하는 완전한 형태의 리눅스 OS가 완성되었습니다.

#### 2.1.3 커널 (Kernel)
* **정의:** 운영체제의 핵심이자 하드웨어를 직접 제어하고 관리하는 '컴퓨터의 최상위 관리자'입니다.
* **셸(Shell)의 필요성:** 사용자는 커널에게 직접 명령을 내릴 수 없습니다. 따라서 중간에서 사용자의 명령을 커널이 이해할 수 있도록 번역해 주는 **통역사 역할의 Shell**이 반드시 필요합니다.

> **💡 명령어 처리 흐름 예시**
> `사용자` ➔ `Shell (ls 입력)` ➔ `Kernel` ➔ `하드웨어 (디스크 조회)` ➔ `결과 화면 출력`

* **커널의 5대 핵심 역할:**
  1. **CPU 관리:** 어떤 프로그램에 CPU 연산 자원을 우선 배정할지 결정
  2. **메모리 관리:** 각 프로그램에 RAM 공간을 안전하게 할당하고 회수
  3. **파일 관리:** 디스크 저장 장치에 파일을 읽고 쓰는 권한 및 구조 제어
  4. **네트워크 관리:** TCP/IP 프로토콜 스택 처리 및 데이터 송수신 관리
  5. **장치 관리:** USB, SSD, 랜카드(NIC), 프린터 등 물리 장치 드라이버 제어

---

### 2.2 우분투 리눅스 배포판
* **배포판(Distribution)이란?** 리눅스 커널 자체만으로는 일반 사용자가 쓸 수 없기 때문에, `커널 + 명령어 + 패키지 관리자 + 설치 프로그램 + GUI`를 하나로 묶어 구성한 완전한 운영체제 패키지입니다. (ex. Ubuntu, Debian, Rocky Linux, AlmaLinux, Fedora 등)

#### 2.2.1 데비안과 우분투
우분투는 데비안(Debian) 리눅스를 모태로 하여 파생된 배포판입니다.
* **Debian 특징:** 철저한 검증을 거치므로 매우 안정적이고 보수적이지만, 패키지 업데이트가 상대적으로 느립니다.
* **Ubuntu 특징:** 초보자도 접근하기 쉬우며, 최신 패키지를 빠르게 제공하고 전 세계적으로 참고할 수 있는 문서(커뮤니티)가 가장 방대합니다.

#### 2.2.2 우분투 배포판 종류
* **Ubuntu Desktop:** GUI(그래픽 화면)가 기본 포함되어 윈도우처럼 마우스로 조작 가능한 버전입니다.
* **Ubuntu Server:** GUI가 제외된 CLI(텍스트 터미널) 기반으로 작동하며, 서버 자원 낭비를 최소화하여 성능을 극대화한 버전입니다.

#### 2.2.3 우분투 버전 규칙
* **버전 명명:** `YY.MM` 형태로 출시 연도와 월을 나타냅니다. (ex. Ubuntu 24.04는 2024년 4월 출시 제품)
* **LTS (Long Term Support):** 2년마다 출시되는 **장기 지원 버전**으로, 성능이 안정적이고 기술 지원 기간이 길어 기업 실무 환경에서는 대부분 LTS 버전을 채택합니다.

#### 2.2.4 Ubuntu 24.04 설치 요구사항
* **CPU:** 2 Core 이상
* **RAM:** 4GB 이상
* **Disk:** 25GB 이상의 여유 공간

#### 2.2.5 우분투 주요 패키지 요약

| 패키지 명 | 주요 역할 및 특징 |
| :--- | :--- |
| **Bash** | 우분투의 기본 셸(Shell)로 명령어 해석 및 스크립트 실행 담당 |
| **OpenSSH** | 원격지에서 리눅스 서버에 안전하게 암호화 접속하기 위한 필수 보안 프로토콜 |
| **systemd** | 리눅스 부팅 시 시스템 서비스(데몬)들을 전체적으로 제어하고 관리하는 최상위 프로세스 |
| **APT** | 우분투 전용 패키지 관리 도구로, 인터넷을 통해 소프트웨어를 쉽게 설치/삭제 가능 |
| **Vim** | CLI 환경에서 가장 널리 쓰이는 강력한 기본 텍스트 편집기 |
| **Netplan** | 우분투의 최신 네트워크 설정 도구 (`/etc/netplan/` 경로의 YAML 파일로 IP/DNS 관리) |
            
## 3장. 서버 구축 시 알아야 할 필수 개념과 명령어

> **개요:** 리눅스 서버 운영의 기본이 되는 전원 관리, 가상 콘솔, 디렉터리 구조, 기본 도구(에디터/마운트) 및 필수 명령어들을 학습합니다.

---

### 3.1 리눅스 운영에서 먼저 알아야 할 개념

#### 3.1.1 시작과 종료
* **시스템 즉시 종료:** `sudo shutdown now`
* **전원 종료:** `sudo poweroff`
* **종료 예약 (10분 뒤):** `sudo shutdown -h +10`
* **예약된 종료 취소:** `sudo shutdown -c`

#### 3.1.2 시스템 재부팅
* **언제 하는가?** 커널 업데이트 적용, 주요 네트워크 설정 변경, 시스템 장애 복구 시 수행합니다.
* **명령어:** `sudo reboot` 또는 `sudo shutdown -r now`

#### 3.1.3 로그아웃 (Logout)
* **개념:** 시스템 자체를 끄지 않고, 현재 로그인된 사용자 세션만 안전하게 종료합니다.
* **명령어:** `exit` 또는 `logout`

#### 3.1.4 가상 콘솔 (Virtual Console)
* 하나의 화면만 사용하는 윈도우와 달리, 리눅스는 하나의 모니터(컴퓨터)에서 여러 개의 독립된 터미널 창을 전환하며 사용할 수 있는 가상 콘솔 기능을 지원합니다.

#### 3.1.5 리눅스 디렉터리 구조 (FHS)
리눅스는 모든 파일과 디렉터리가 최상위 루트(`/`)를 기점으로 역트리(Tree) 구조를 이룹니다.

| 디렉터리 | 주요 역할 및 저장 데이터 |
| :--- | :--- |
| **`/`** | **최상위 루트 디렉터리**, 모든 절대 경로의 시작점 |
| **`/home`** | 일반 사용자들의 개인 홈 디렉터리 공간 (ex. `/home/ubuntu`) |
| **`/root`** | 최상위 관리자(root) 전용 홈 디렉터리 |
| **`/etc`** | 시스템 및 각종 애플리케이션의 **핵심 설정 파일** 저장소 |
| **`/var`** | 시스템 로그, 데이터베이스 파일 등 **동적으로 변하는 데이터** 저장 |
| **`/tmp`** | 시스템 및 프로그램이 사용하는 임시 파일 저장소 (부팅 시 초기화 가능) |
| **`/usr`** | 일반 사용자들이 실행하는 프로그램 및 유틸리티 저장 설치 경로 |
| **`/bin`** | 시스템의 필수 기본 명령어 모음 (`ls`, `cp`, `mv`, `cat` 등) |
| **`/dev`** | 하드웨어 장치들을 파일 형태로 관리하는 장치 파일 저장소 |
| **`/proc`** | RAM에 상주하는 커널 정보 및 실시간 시스템 정보(가상 파일 시스템) |

#### 3.1.6 런레벨 (Runlevel)
* **개념:** 과거 리눅스 시스템의 부팅 및 운영 모드를 정의하던 개념입니다. 현재 우분투 등 최신 리눅스에서는 **`systemd` 인프라의 Target 개념으로 대체**되어 사용됩니다.

#### 3.1.7 자동 완성과 히스토리
* **`TAB` 키:** 파일명, 디렉터리명, 명령어 등을 입력할 때 자동으로 완성해 주는 필수 기능입니다.
* **`history`:** 이전에 실행했던 명령어 기록들을 번호 순으로 조회합니다.

#### 3.1.8 에디터 사용 (설정 및 스크립트 작성)
서버 관리자에게 에디터 사용법은 설정 파일 수정, 로그 확인, 스크립트 작성을 위한 필수 기술입니다.
* **`nano`:** 다루기 쉽고 직관적인 텍스트 에디터입니다.
* **`vi` / `vim`:** 리눅스 환경의 실무 표준 에디터입니다. (`vim`은 향상된 버전을 의미)
  * **입력 모드 전환:** `i` (Insert)
  * **명령 모드 복귀:** `ESC`
  * **저장 후 종료:** `:wq`
  * **저장하지 않고 강제 종료:** `:q!`

#### 3.1.9 도움말 사용법
* **`man [명령어]`:** 특정 명령어의 사용법, 옵션, 매뉴얼 페이지를 상세히 확인합니다.

#### 3.1.10 마운트(Mount)와 스토리지 활용
* **마운트 (Mount):** 리눅스에서는 새 디스크, CD/DVD, USB 등의 물리 장치를 디렉터리에 연결(인식)해 주어야 사용이 가능합니다.
  * *장치 연결 예시:* `/dev/sdb1` 디스크를 `/data` 폴더에 연결할 때
    ```bash
    mount /dev/sdb1 /data
    ```
  * *장치 해제 예시:* 연결된 마운트를 안전하게 해제할 때
    ```bash
    umount /data
    ```
#### 3.1.11 리눅스에서 ISO 파일 생성
* **개념:** CD/DVD 등 디스크 전체 데이터 구조를 백업하거나 배포하기 위해 하나의 가상 파일(`.iso`)로 압축 및 생성한 아카이브 이미지 파일입니다.

 ### 3.2 사용자와 그룹 관리

#### 3.2.1 사용자와 그룹의 개념
* **사용자 (User):** 서버에 로그인하는 개별 계정입니다. (ex. `root`, `ubuntu`, `nginx`, `mysql` 등)
* **그룹 (Group):** 여러 사용자를 효율적으로 관리하기 위해 하나로 묶는 단위입니다.
* **root:** 시스템의 최고 관리자로 모든 파일에 접근하고, 설정 변경 및 서비스 제어를 제한 없이 수행할 수 있습니다.
* **sudo:** 일반 사용자가 최상위 권한(`root`)이 일시적으로 필요할 때 빌려 쓰는 명령어입니다.
* **사용자 및 그룹 정보가 저장되는 핵심 파일:**
  * `/etc/passwd` : 사용자 계정의 이름, UID, 홈 디렉터리, 기본 셸 정보 저장
  * `/etc/shadow` : 사용자 비밀번호가 안전하게 암호화(해시)되어 저장되는 파일
  * `/etc/group` : 시스템에 생성된 그룹 정보와 그룹에 속한 사용자 목록 저장

#### 3.2.2 사용자와 그룹 관련 명령어

| 작업 내용 | 명령어 | 비고 |
| :--- | :--- | :--- |
| **사용자 생성** | `sudo adduser newUser` | 대화식으로 비밀번호 및 정보 입력 |
| **사용자 삭제** | `sudo deluser newUser` | 계정만 삭제 |
| **사용자 완전 삭제** | `sudo deluser --remove-home newUser` | 사용자의 홈 디렉터리까지 함께 삭제 |
| **사용자 전환** | `su - newUser` | 해당 사용자의 환경변수를 적용하며 전환 |
| **현재 사용자 확인** | `whoami` | 현재 세션을 사용하는 사용자명 출력 |
| **로그인 사용자 확인** | `who` | 현재 서버에 로그인 중인 전체 사용자 목록 |
| **그룹 생성** | `sudo groupadd developers` | `developers` 이름의 그룹 생성 |
| **그룹 삭제** | `sudo groupdel developers` | 그룹 삭제 |
| **사용자를 그룹에 추가**| `sudo usermod -aG developers newUser` | 기존 그룹 구성을 유지한 채 보조 그룹 추가 |
| **sudo 권한 부여** | `sudo usermod -aG sudo newUser` | `sudo` 그룹에 추가하여 관리자 권한 부여 |

---
### 3.3 파일 속성 (리눅스 보안의 핵심)

#### 3.3.1 소유권과 허가권
* **확인 방법:** `ls -l` 명령어로 상세 권한 출력
* **파일 권한 구조 예시:** `-rwxr-xr--`
  * 맨 앞 글자(-): 파일 형태 (디렉터리는 d, 일반 파일은 -)
  * 앞의 세 글자(rwx): 소유자(User) 권한
  * 중간 세 글자(r-x): 그룹(Group) 권한
  * 뒤의 세 글자(r--): 기타 사용자(Others) 권한
  * 권한 의미: r = Read(4), w = Write(2), x = Execute(1)

* **chmod (권한 변경):** 숫자의 합을 이용해 설정합니다.
  * 예시: `chmod 755 test.sh` (소유자 rwx, 그룹 r-x, 기타 r-x 권한 부여)
* **chown (소유자 변경):** `sudo chown kim test.txt` (파일 소유자를 kim으로 변경)
* **chgrp (그룹 변경):** `sudo chgrp developers test.txt` (파일 소유 그룹을 developers로 변경)

#### 3.3.2 링크 (Link)
* **정의:** 하나의 파일을 여러 다른 경로에서 접근할 수 있도록 연결하는 기술입니다.
* **심볼릭 링크 (Symbolic Link):** 윈도우의 '바로가기 아이콘'과 같은 개념입니다.
  * 생성: `ln -s /data/app /app`
  * 특징: 원본 파일이 삭제되거나 이동하면 링크가 깨져서 사용할 수 없게 됩니다.
* **하드 링크 (Hard Link):** 원본 파일의 실제 데이터 공간을 직접 같이 공유합니다.
  * 생성: `ln file1 file2`
  * 특징: 원본 파일이 삭제되어도 하드 링크된 파일이 남아있다면 데이터를 그대로 유지하고 사용할 수 있습니다.

---

### 3.4 리눅스 관리자를 위한 필수 명령어

#### 3.4.1 dpkg (데비안 패키지 관리 도구)
* **특징:** 우분투의 로컬 설치 파일인 `.deb` 파일을 관리합니다.
* **주요 명령어:**
  * 패키지 설치: `sudo dpkg -i nginx.deb`
  * 설치 목록 조회: `dpkg -l`
* **단점:** 프로그램 설치 시 필요한 의존성(Dependency) 문제를 스스로 해결하지 못해 에러가 발생할 수 있습니다.

#### 3.4.2 apt (고급 패키지 도구)
* **특징:** 원격 저장소에서 패키지를 다운로드하며, 의존성 문제를 자동으로 해결해 줍니다.
* **주요 명령어:**
  * 패키지 설치: `sudo apt install nginx`
  * 패키지 삭제: `sudo apt remove nginx`
  * 저장소 인덱스 업데이트: `sudo apt update`
  * 시스템 패키지 전체 업그레이드: `sudo apt upgrade`

#### 3.4.3 파일 압축과 묶기
* **tar (파일 묶기):**
  * 하나로 묶기: `tar cvf backup.tar data/`
  * 묶음 풀기: `tar xvf backup.tar`
* **gzip (파일 압축):**
  * 파일 압축: `gzip file.txt`
* **tar + gzip (실무 최강 조합):** 옵션 `z`를 사용하여 한 번에 묶고 압축합니다.
  * 압축하기: `tar czvf backup.tar.gz data/`
  * 압축해제: `tar xzvf backup.tar.gz`

#### 3.4.4 파일 및 내용 검색 (find / grep)
* **find (파일 위치 검색):** 디렉터리 경로 내에서 조건에 맞는 파일을 찾아냅니다.
  * 이름 검색: `find / -name nginx.conf`
  * 크기 검색: `find / -size +100M` (100MB 이상 파일 검색)
* **grep (파일 내용 검색):** 파일 내부의 텍스트 지정 문자열을 검색합니다.
  * 예시: `grep ERROR app.log` (app.log 파일에서 ERROR 문구가 포함된 라인만 출력)

#### 3.4.5 시스템 설정 및 환경변수
* **환경변수:** 운영체제나 프로그램이 실행될 때 참조하는 동적 값들입니다.
* **주요 확인 및 추가 방법:**
  * 환경변수 확인: `echo $PATH`
  * 환경변수 임시 추가: `export PATH=$PATH:/home/user/bin`
* **대표적인 환경변수:** `HOME`, `PATH`, `USER`, `HOSTNAME`

#### 3.4.6 예약 작업 관리 (cron과 at)
* **cron (주기적 정기 실행):** 정해진 주기마다 반복하여 명령을 실행합니다.
  * 설정 명령어: `crontab -e`
  * 예시 설정: `0 3 * * * backup.sh` (매일 새벽 3시 0분에 해당 백업 스크립트 실행)
* **at (1회성 예약 실행):** 지정된 미래의 한 시점에 딱 한 번만 작업을 수행합니다.
  * 예시 설정: `at 18:00` 입력 후 실행할 명령어(shutdown now) 작성 후 저장 (당일 18시에 시스템 종료 수행)

### 3.5 네트워크 관련 설정과 명령어

#### 3.5.1 네트워크 확인
* **IP 주소 확인:** `ip a` 또는 `ip address`
  * 시스템에 할당된 네트워크 인터페이스와 IP 정보를 출력합니다.

#### 3.5.2 통신 확인 (ping)
* **명령어:** `ping google.com`
* **역할:** 목적지 서버와 패킷을 주고받으며 상호 통신이 가능한 상태인지 체크합니다.
* **실무 활용:** 서버 접속이 불가능하거나 네트워크가 끊겼을 때 가장 먼저 통신 상태를 진단하는 용도로 사용합니다.

#### 3.5.3 DNS 확인 (nslookup)
* **명령어:** `nslookup google.com`
* **역할:** 도메인 이름을 IP 주소로 올바르게 변환(해석)하는지 확인합니다.
* **실무 활용:** 목적지 서버의 IP 주소로는 접속이 잘 되는데, 도메인 주소로만 접속이 안 된다면 네임서버(DNS) 설정을 의심하고 이 명령어로 검증합니다.

#### 3.5.4 라우팅 확인 (ip route)
* **명령어:** `ip route`
* **역할:** 현재 설정된 기본 게이트웨이(Gateway) 경로를 확인합니다.
* **실무 활용:** 내부 통신은 정상인데 외부 인터넷망 연결만 완전히 차단된 경우, 게이트웨이 경로 설정의 누락 여부를 판단하기 위해 확인합니다.

---

### 3.6 프로세스 관련 개념과 명령어

#### 3.6.1 프로세스 관리 기본
* **프로세스(Process)란?** 메모리에 적재되어 현재 실행 중인 프로그램을 의미합니다.
* **프로세스 확인:** `ps` 또는 `ps -ef` (시스템의 전체 프로세스 세부 정보 출력)
* **특정 프로세스 찾기:** `ps -ef | grep nginx`
* **실시간 모니터링:** `top` (CPU 및 메모리 사용량 실시간 확인)
* **프로세스 종료:** `kill PID` (정상 종료 요청)
* **프로세스 강제 종료:** `kill -9 PID` (시그널을 이용한 즉시 강제 종료)

#### 3.6.2 파이프, 필터, 리디렉션
* **파이프 ( | ):** 앞 명령어의 실행 결과를 뒤 명령어의 입력 값으로 전달합니다.
  * 예시: `ps -ef | grep nginx`
* **필터 (Filter):** 필요한 데이터만 걸러내거나 정렬하는 명령어들입니다. (`grep`, `sort`, `wc` 등)
  * 예시: `grep ERROR log.txt` (로그 파일에서 ERROR가 포함된 줄만 검색)
* **리디렉션 (Redirection):** 화면에 출력될 결과를 파일로 방향을 바꾸어 저장합니다.
  * `>` (덮어쓰기): `ls > result.txt` (기존 내용을 지우고 새로 저장)
  * `>>` (이어쓰기): `echo hello >> result.txt` (기존 내용 뒤에 추가)

#### 3.6.3 프로세스, 데몬, 서비스의 차이
* **프로세스:** 실행 중인 모든 프로그램의 통칭입니다.
* **데몬 (Daemon):** 사용자의 입력 없이 백그라운드에 상주하며 항상 실행되는 프로그램입니다.
* **서비스 (Service):** 리눅스의 최상위 관리 시스템인 `systemd`에 의해 통제되고 관리되는 데몬 프로그램을 말합니다.

---

### 3.7 서비스와 소켓

#### 3.7.1 서비스 (Service)
* **특징:** 시스템 백그라운드에서 항상 실행되고 있는 형태입니다. 요청이 들어오면 즉시 응답할 수 있어 빠르지만, 실행을 유지하는 동안 지속적으로 메모리를 차지한다는 단점이 있습니다.
* **주요 관리 명령어 (systemctl):**
  * 상태 확인: `systemctl status nginx`
  * 서비스 시작: `systemctl start nginx`
  * 서비스 중지: `systemctl stop nginx`
  * 서비스 재시작: `systemctl restart nginx`

#### 3.7.2 소켓 (Socket)
* **특징:** 상시 실행되지 않고 대기하다가, 외부에서 실제 연결 요청이 들어오는 그 시점에만 서비스를 동적으로 실행시키는 방식입니다. 자원을 효율적으로 아낄 수 있습니다.
            
      
      
      
              
  
  
                   
            
            
       











         
         
         
